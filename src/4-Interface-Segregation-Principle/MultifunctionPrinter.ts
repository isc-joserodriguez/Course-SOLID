import IPrint from "./IPrint";
import IScan from "./IScan";

export default class MultifunctionPrinter implements IPrint, IScan {
  print(): void {
    // Asume real and functional code
  }
  getPrintSpoolDetails(): void {
    // Asume real and functional code
  }
  scan(): void {
    // Asume real and functional code
  }
  scanPhoto(): void {
    // Asume real and functional code
  }
}
